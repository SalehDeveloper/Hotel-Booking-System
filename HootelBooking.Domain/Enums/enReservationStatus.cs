namespace HootelBooking.Domain.Enums
{
    public enum enReservationStatus
    {
        // still not pay 
        PENDING = 1,
        CONFIRMED = 2,
        CHECKEDIN = 3,
        COMPLETED= 4,
        CANCELED = 5 
    }
}
